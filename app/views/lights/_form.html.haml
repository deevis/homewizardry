= form_for @light, html: { class: "form-horizontal", role: "form" } do |f|
  -if @light.errors.any?
    .alert.alert-danger.alert-dismissable{role: "alert"}
      %button.close{type: "button", data: {dismiss: "alert"} }
        %span{aria: {hidden: "true"} } &times;
        %span.sr-only Close
      %h4= "#{pluralize(@light.errors.count, "error")} prohibited this light from being saved:"

      %ul
        - @light.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :entity_id, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :entity_id, class: 'form-control'
  .form-group
    = f.label :name, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :name, class: 'form-control'
  .form-group
    = f.label :room, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.collection_select :room_id, Room.all, :id, :name
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit class: 'btn btn-primary'
